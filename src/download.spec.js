import download from "./download";

describe("NPM download", () => {
  it("should get latest version of Candy-board", () => {
    // Given
    const functionCalled = async () => await download("samples/badInput.json");

    // Then
  })
})